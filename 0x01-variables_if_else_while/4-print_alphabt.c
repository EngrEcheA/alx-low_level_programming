#include <stdio.h>
/**
 * main - this is for this program
 * Return: always 0
 */

int main(void)
{
	char i;

	for (i = 'a'; i <= 'z'; i++)
	{
		if (i != 'e' && i != 'q')
		putchar(1);
	}
	putchar('\n');
return (0);
}
