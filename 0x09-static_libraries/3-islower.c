#include "main.h"

/**
 * _islower - Checks for lower character
 * @x: character to be checked
 * Return: 1 if x is lowercase, 0 if otherwise
 */

int _islower(int x)
{
	return (x >= 'a' && x <= 'z');
}
